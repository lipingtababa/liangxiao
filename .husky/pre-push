#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 运行验收测试前的检查
echo "🧪 运行验收测试..."

# 运行快速验收测试（只运行关键场景）
npm run test:acceptance -- --tags "@critical or @smoke" --fail-fast

if [ $? -ne 0 ]; then
  echo "❌ 验收测试失败！请修复后再推送。"
  exit 1
fi

echo "✅ 验收测试通过！"